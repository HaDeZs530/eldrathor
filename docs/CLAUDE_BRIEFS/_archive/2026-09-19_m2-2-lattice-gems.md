# BRIEF — M2 lock 2: lattice engine + class gems live (drop, equip, gem-bound fragments, lattice screen)
**Status:** DONE (PR #70, 2026-09-19, Claude Code) — one lock conflict flagged, see the PR · **Date:** 2026-09-19 · **Author:** Claude Design Chat  
**Specs (authoritative):** `docs/Eldrathor_Growth_Model_Lock.md` §1, §4, §5 · `docs/Eldrathor_ClassGems_Live_Lock.md` §3 (numbers table), §4 (drops), §5 (equip / crossing / matching) · `docs/Eldrathor_ClassGemTrees_Lock.md` (facet contents per class). Bump `SAVE_VERSION`. Replaces the withdrawn row-tree brief; the words "talent" and "tree" do not appear in UI text — it's *facets* and *the lattice*.

## Deliverables
1. **Lattice engine** `app/src/lattice/engine.js` — `LatticeDef` / `LatticeState`, adjacency-only gating from `neighbors`, exclusive finisher group, `canImbue / imbue / derive / summary`, pure and unit-tested. Generic (no gem assumptions).
2. **Layout** `app/src/lattice/hexLayout.js` — axial hex coordinates for Core + ring1 (6) + ring2 (12) + ring3 (18) + 4 compass finisher hexes on the outer edge; procs = two ring-2 hexes opposite each other. One shared geometry; each class only changes facet contents.
3. **Class lattices** `app/src/lattice/classGems.js` — four `LatticeDef`s: July contents mapped ring by ring (Row 1 → ring 1, Row 2 → ring 2, procs → ring-2 specials, Row 4 → ring 3, finishers → compass points). All magnitudes from a single `GEM_TUNING` table (ClassGems_Live §3; imbue cost `20 × 1.12^n`; finisher swap 200 ❖ + 2 fragments).
4. **Gem items** — `{ id, kind:'gem', gemClass, lattice: LatticeState, fragments: { unspent, imbued }, name }`. Drops: rares 20%, bosses 35%, named +10%, class uniform, no pity. Bag row `Tank Gem  [12/40]  3 frag  Equipped · Kessa`; gem sheet with Equip / Open lattice.
5. **Gem-bound fragments** — on any level-up (fight or Train), the worn gem gains 1 unspent fragment; unequipped gems gain nothing; fragments persist through unequip/transfer. Results and run log print `Kessa's Tank Gem +1 fragment`.
6. **Equip + effects** — Gem slot live on the character sheet (drop the "Coming" tag). Crossing grants the Core ability; matching amplifies the archetype's personal innate by `1 + 0.5 × imbued/40`; stat facets, procs and finishers flow into `derive` and `simulate` as `proc` / `finisher` feed events; third ability button on party cards. Resonator always crosses.
7. **Lattice screen** (Mind View, §4) — SVG hex lattice centred on the Core, pinch-zoom + pan (same camera rules as the route map: eased, never a cut), lit / available / unreachable facet states, header (gem, wearer, imbued/40, unspent fragments, Worldvein, live readout; "Equip to grow" when unworn), facet sheet with per-level effect, cost (1 fragment + ❖), **Imbue** with a stated reason when disabled, finisher swap. Test-numbers mode shows derive output. Open from the Gem slot and from the gem sheet.
8. **Tests (the spec):** adjacency gating (a ring-3 facet is unreachable until a touching ring-2 facet is imbued); exclusive finisher; cost curve; every facet's effect at each level; crossing grants exactly one Core ability; matching multiplier and no second ability; fragments accrue only to the worn gem and survive transfer + save round-trip; drop rates; balance gates still pass with no gems.

PR: `feat(gems): lattice engine, class gems live, gem-bound fragments, lattice screen`
