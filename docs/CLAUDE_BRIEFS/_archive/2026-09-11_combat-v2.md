# BRIEF — Combat v2 (pre-rolled script resolver + eye-candy fight screen)

**Status:** READY for Claude Code — do AFTER `2026-09-11_loop-cleanup-and-stat-spine.md`  
**Date:** 2026-09-11 · **Author:** Claude Design Chat  
**Spec:** `docs/Eldrathor_Combat_v2_Lock.md` (authoritative — implement it, don't reinterpret it)

## Deliverables
1. `app/src/combat/simulate.js` — pure function `simulateFight({party, enemies, seed}) → {events, result, stats}`. 100 ms ticks, formulas from spec §2–§4, innates §3, deterministic by seed. Unit-testable; add `app/src/combat/simulate.test.js` with 3 cases (W1 trash wins, W1 boss wipes a level-1 party, Aegis taunt redirects targeting).
2. `app/src/combat/derive.js` — `deriveStats(adventurer)` per spec §2 using `ARCHETYPE_SEEDS` + weapon table. Replace the legacy hp/atk/def path.
3. `app/src/combat/enemies.js` — `spawnEnemies(worldTier, nodeType, rareFlag)` per §4.
4. `FightScreen.jsx` — playback per §5–§6: stage, three party cards with mana bars + innate buttons that flash on fire, feed, 1×/2×/Skip. Playback is a timer over `events`, scaled by speed.
5. `LootResults.jsx` — per §7 (per-Adventurer numbers, rating 1–100, Attune Vein line, wipe variant).
6. `data.js` — weapon table from spec §2; remove `atk/def` from `ARCHETYPES` once nothing reads them.
7. Delete `resolveFight` / `rollLoot` from `combat.js` or reduce `combat.js` to re-exports.

## Constraints
- No miss/evasion. No damage types. Innates auto-fire — never a tap. Tab bar stays visible.
- Numbers exactly as spec; anything the spec doesn't give → `// DESIGN-OPEN:` with your placeholder named in the PR.
- Keep the mind-view theme + crossfade. Glyphs only, no art.

## Test plan (phone portrait)
- W1 normal node: fight ends 10–20 s at 1×; innate buttons visibly flash; feed shows crits in color.
- W1 boss with a fresh level-1 party: party wipes; results wipe card → Veinharbor; map resets.
- 2× halves wall time; Skip lands on results immediately.
- `npm run build` clean; tests pass.

## PR
Title `feat(combat): v2 pre-rolled resolver + fight screen`. Summary + test notes. Then add DONE line to `docs/SESSION_LOG.md`.
