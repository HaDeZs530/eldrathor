# BRIEF — UI shell: island zoom toggle + dotted path, Mind-view scale-up, Help (?) and Menu (☰) on every screen

**Status:** READY · **Date:** 2026-09-12 · **Author:** Claude Design Chat  
**Spec:** `docs/Eldrathor_UI_Shell_Lock.md` (authoritative)

## Deliverables
1. Island map: + / − zoom toggle (Close / Overview = fit height), crossfade, camera focal point preserved. Restore the dotted Catmull-Rom spline per `Eldrathor_Island_Path_Lock.md`.
2. `theme/`: add `--mv-text --mv-label --mv-num --mv-tap` and card/feed sizes; apply across route map HUD, scout card, fight, results.
3. `components/shell/HelpSheet.jsx`, `MenuSheet.jsx`, `ScreenHeaderActions.jsx` (☰ + ?). Mount in every screen header. `app/src/help/helpText.js` seeded with the copy in the lock, keyed by screen id. Menu items per lock incl. Extract-with-confirm during a run.
4. Sheets follow Dual-mode theming (warm on reality, Mind-view on projection).

## Test plan
Every screen shows ☰ and ?; ? opens the right copy; menu navigates; zoom toggle works and path is visible in both states; fight screen text is legibly larger on a 390 px phone; build green.

PR title: `feat(ui): island zoom + path, mind-view scale, help & menu sheets`
