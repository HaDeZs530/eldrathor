# BRIEF — Item numbers: three-avenue formula, tier-named armor, boss-named drops, save wipe policy
**Status:** READY · **Date:** 2026-09-17 · **Author:** Claude Design Chat · **Spec:** `Eldrathor_Item_Model_Lock.md` §1 (power), §2 (names), §9 (save policy)
1. Replace `TIER_MULT` (1.8^(T−1)), `RARITY_MULT` (1.158^rung), `ratingScale` (1 + 0.15×(r−1)/99), `empowerScale` (1 + e/400). Tests: Artifact T1 == Uncommon T2 at equal rating (within 1%); Legendary T1 == Common T2; no T1 item at any rarity/rating/empower beats a T3 Common rating 1; rating 100 = +15%; empower 100 = +25%.
2. Armor names by tier prefix (Gullwatch/Saltcliff/Quay/Serpent/Forge/Bastion/Worldforge + type). Boss weapon drops pull from `data/bossWeaponNames.js` (3 per boss; placeholders `"<Boss>'s <Type>"` until the Design Chat pushes the pools).
3. Save policy §9: bump `SAVE_VERSION`, drop the migration chain (keep the loader's version check + "Save reset for a game update" notice + export/import). Remove the T1 legacy-migration code.
4. **Enemy → Adventurer damage is visible.** The fight feed logs every enemy hit on a party member (`Shore Drake hits Kessa for 14`), crits in colour, with the same event types as party hits; the stage flashes the struck party card. Results per-Adventurer "damage taken" already exists — make sure it matches the feed's sum (test).
5. **Test-numbers mode** (Settings → "Show test numbers", off by default, persisted): when on — every ItemRow/ItemSheet shows its computed power contribution and the multiplier breakdown (tier × rarity × rating × empower); the character sheet shows the 9 derived stats with source breakdown (seed / level / gear); the scout/reveal card shows the enemy stat block (hp, dmg, interval, mit, tier, depth mult); each feed line appends raw → mitigated (`14 (22 − 36%)`); the results card adds party DPS, enemy DPS taken, fight seed. Nothing else changes; no gameplay effect.
6. Re-run the §8 balance gates; retune `ENEMY_TUNING` (never the item formula) if they move; report final numbers.
PR: `feat(items): three-avenue power formula, tier-named armor, boss-named drops, enemy damage in feed, test-numbers mode, save wipe`
