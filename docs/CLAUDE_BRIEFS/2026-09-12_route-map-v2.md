# BRIEF — Route map v2: web generation, scout/engage/leave, 5 node types, roaming rares, seal, respawns, Rally party-edit + lore

**Status:** READY · **Date:** 2026-09-12 · **Author:** Claude Design Chat  
**Specs (authoritative):** `docs/Eldrathor_RouteMap_v2_Lock.md`, `docs/Eldrathor_Island_Areas_Lock.md`

## Deliverables
1. **`data.js`** — replace `WORLDS` with `AREAS` (9 entries) from the Island Areas lock: id, pin, name, tier, character, boss name, lore. Island map hotspots = pins 2–10, all tappable when unlocked; harbor stays pin 1.
2. **`map/genTerritory.js`** — implement lock §1: 2–3 reveals per clear, cross-linking so ≥4 loops and 30–40% nodes with ≥3 edges, depth computed by BFS and stored per node; 5 node types incl. Sanctuary (2–4, placement rules); 2–3 rares; boss sealed. Add `depth`, `scouted`, `sanctuaryUsed` fields.
3. **Scout flow** — `RouteMapScreen`: tapping a frontier node opens `ScoutCard` (type, count, threat band via a quick `simulateFight` dry-run: Easy ≥80% win / Even / Hard / Deadly ≤20%, yield for crystal/sanctuary). Engage → fight or sanctuary event; Leave → close. Scouting ticks the node-action clock.
4. **Sanctuary event screen** — full heal/mana/revive + pick one of the three bonuses (buttons). Mind-view.
5. **Rares roam + seal** — per lock §4. Boss node chained icon until seal breaks; then path highlight.
6. **Respawns + named variants** — per lock §5; named = ×1.3 stats, +1 loot roll at +1 tier; gold rim.
7. **Depth difficulty** — enemies spawn with `1 + 0.5×depth` multiplier (not rares/boss).
8. **Run end** — Extract with confirm; map-clear bonus; wipe path unchanged.
9. **Rally screen** — lore panel + tappable party slots with roster picker sheet + Explore.
10. Combat v2 enemy spawn uses area tier instead of world tier.

## Constraints
No flee inside fights. Tab bar stays. Mind-view scale vars from the UI Shell brief if it lands first; otherwise use 16/13/18 px and 48 px taps directly.

## Test plan (phone)
Fresh map shows ≥2 frontier nodes after first clear and paths rejoin later; scout card appears and Leave works; a rare visibly moves after 2 actions; boss stays chained until rares die; a cleared node visibly respawns in a long run; sanctuary heals and revives; Rally lets you swap an Adventurer; lore text shows for all 9 areas; `npm run build` + tests green.

PR title: `feat(route): route map v2 — web gen, scout/engage, node types, rares+seal, respawns, rally`
