# Claude briefs

Active coding tasks for **Claude**. Boss (Grok Bot) writes these; Claude implements.

- Newest brief = next build priority unless Anthony says otherwise.
- Filename: `YYYY-MM-DD_short-slug.md`
- When a brief is merged, move a one-line note into `docs/GROK_BOT_SESSION_LOG.md` and mark the brief **DONE** at the top (keep the file for history).
